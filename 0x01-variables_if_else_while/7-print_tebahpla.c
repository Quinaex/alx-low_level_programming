#include <stdio.h>
/**
 * main - a program that prints alphabets in reverse
 * Return: 0 if successful
 */

int main(void)

{
	char x;

	x = 'z';
	while
		(x >= 'a') {
			putchar(x);
			x--;
		}
	putchar('\n');
	return (0);
}
