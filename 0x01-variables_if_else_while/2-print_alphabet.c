#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase.
 * Return: 0 if successful
 */

int main(void)

{
	char x;

	x = 'a';
	while
		(x <= 'z') {
			putchar(x);
			x++;
		}
	putchar('\n');
	return (0);
}
