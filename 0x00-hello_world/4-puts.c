#include <stdio.h>
/**
 * main - Prints a line with puts function instead of printf.
 * Return: 0 (success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
