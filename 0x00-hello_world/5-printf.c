#include <stdio.h>
/**
 * main - Prints a line using printf instead of puts
 * Return: 0 if successful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
